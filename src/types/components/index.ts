export interface SosmedType {
  icon: any;
  value: string;
}

export interface RestaurantCardType {
  image: string;
  title: string;
  button: string;
}

export interface SponsorType {
  image: string;
  title: string;
  desc: string;
}
